\documentclass[11pt]{article}

% ------------------------------------------------------------------------------------------------------------------------
% PACKAGES
% Vous pouvez ajouter des packages si besoin
% Éviter les packages trop exotiques ou connus pour poser des problèmes / incompatibilités
% ------------------------------------------------------------------------------------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{siunitx}
\usepackage[dvipsnames]{xcolor}
\sisetup{separate-uncertainty,
locale = FR, scientific-notation = false, exponent-product = \times, inter-unit-product = \ensuremath{{}\cdot{}}}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\usepackage[version=4]{mhchem} %affichage des formules chimiques
\usepackage{pgfplots} %plots directement dans latex
\pgfplotsset{height = 7cm,width=12cm,compat=1.9}
% \usepgfplotslibrary{external}
%\tikzexternalize
\usetikzlibrary{angles, arrows.meta, quotes,calc}
\usepackage{float}
\floatplacement{figure}{H}%placement par défaut des figures !h!tbp
\usepackage[%
      a4paper,%
      textwidth=16cm,%
      top=2cm,%
      bottom=2cm,%
      headheight=25pt,%
      headsep=12pt,%
      footskip=25pt]{geometry}%
\usepackage[french]{babel}
\parindent0pt

\usepackage{caption}
\captionsetup{
    labelformat=empty,
    width = .93\textwidth
}

\usepackage{hyperref}

% ------------------------------------------------------------------------------------------------------------------------
% MACROS
% Ne rajouter que des macros réellement nécessaires afin de réduire la probabilité de conflit
% ------------------------------------------------------------------------------------------------------------------------
% Ensembles mathématiques
\def		\K	{	\ensuremath	{\mathbb	{K}	}	}
\def		\N	{	\ensuremath	{\mathbb	{N}	}	}
\def		\NN	{	\ensuremath	{\mathbb	{N}	}	}
\def		\Z	{	\ensuremath	{\mathbb	{Z}	}	}
\def		\Q	{	\ensuremath	{\mathbb	{Q}	}	}
\def		\R	{	\ensuremath	{\mathbb	{R}	}	}
\def		\RR	{	\ensuremath	{\mathbb	{R}	}	}
\def		\C	{	\ensuremath	{\mathbb	{C}	}	}

% Guillemets français et anglais (simples et doubles)
\newcommand{\frquotes}[1]{\og{#1}\fg}
\newcommand{\ensquotes}[1]{`{#1}'}
\newcommand{\enquotes}[1]{``{#1}''}

% Valeurs absolues
\newcommand{\abs}[1]{\ensuremath{\left|{#1}\right|}}

% ------------------------------------------------------------------------------------------------------------------------
% OPÉRATEURS
% ------------------------------------------------------------------------------------------------------------------------
\DeclareMathOperator{\cotan}{cotan}


% ------------------------------------------------------------------------------------------------------------------------
\newcounter{qNum}

\newif\ifdraft
% Le booléen suivant définit si on est en mode draft (avec affichage des réponses vraies/fausses et méta-données) ou production (cases)
% Commenter pour le mode production ; dé-commenter la ligne \draftrue dans le headers.tex pour voir les détails
\drafttrue

\ifdraft
    \newenvironment{question}[4]
    	{\noindent\rule{\textwidth}{0.5pt}\\{\bf Question~:} \refstepcounter{qNum}\theqNum \par{\bf SF~:} #1 \par{\bf Thème~:} #2 \par{\bf Niveau~:} #3 \par{\bf Dépendance~:} #4 \par\noindent\rule{\textwidth}{0.25pt}\\}
    	{ }
    
    \newenvironment{reponses}
    	{\begin{description}}
    	{\end{description}}
\else
    \newenvironment{question}[4]
    	{\refstepcounter{qNum} \subsubsection{ Question \theqNum} \noindent\rule{.5\textwidth}{0.25pt}\\ {\bf SF~:} #1 \par{\bf Thème~:} #2\par{\bf Niveau~:} #3 \par{\bf Dépendance~:} #4 \par \noindent\rule{.5\textwidth}{0.25pt}\\}
    	{ }

    
    \newenvironment{reponses}
    	{\renewcommand{\descriptionlabel}[1]{$\square$} \begin{description}  }
    	{\end{description}}

\fi

\begin{document}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=2$",sloped] ++ (600.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-1.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=-\sqrt{3}$"] ++ (90:-1.73);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $-1$ et $\theta$ = $10\pi/3$
    \item[false] $x$ = $-\sqrt{2}$ et $\theta$ = $\pi$
    \item[false] $x$ = $-\sqrt{2}$ et $\theta$ = $-5\pi/6$
    \item[false] $x$ = $-\sqrt{2}$ et $\theta$ = $-3\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour rayon 22. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $33\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[false] $x$ = $33\sqrt{3}$ et $\theta$ = $\pi/4$
    \item[true] $x$ = $-22$ et $\theta$ = $11\pi$
    \item[false] $x$ = $33\sqrt{3}$ et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (180.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $y=0$};
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $2\sqrt{3}$ et $\theta$ = $\pi/6$
    \item[true] $x$ = $-2$ et $\theta$ = $\pi$
    \item[false] $x$ = $2\sqrt{3}$ et $\theta$ = $\pi/4$
    \item[false] $x$ = $2\sqrt{3}$ et $\theta$ = $\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=3$, $y=0$ };
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 6 et $\theta$ = $\pi/3$
    \item[true] $OM$ = 3 et $\theta$ = $0$
    \item[false] $OM$ = 3 et $\theta$ = $\pi/6$
    \item[false] $OM$ = 3 et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(4, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $0$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $2\sqrt{2}$ et $OM$ = 4
    \item[true] $y$ = $0$ et $OM$ = 4
    \item[false] $y$ = $2\sqrt{3}$ et $OM$ = 4
    \item[false] $y$ = $6$ et $OM$ = 12
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $5\pi$. Que valent le rayon $OM$ et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[true] $x$ = $-49$ et $OM$ = 49
    \item[false] $x$ = $49\sqrt{2}/2$ et $OM$ = 49
    \item[false] $x$ = $147\sqrt{3}/2$ et $OM$ = 147
    \item[false] $x$ = $49$ et $OM$ = 98
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (540.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$};
    \pic ["$\theta=3\pi$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $3\sqrt{2}/2$ et $OM$ = 3
    \item[false] $x$ = $3$ et $OM$ = 6
    \item[false] $x$ = $9\sqrt{3}/2$ et $OM$ = 9
    \item[true] $x$ = $-3$ et $OM$ = 3
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (660.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x=1$"] ++ (0:1.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=-\sqrt{3}$"] ++ (90:-1.73);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 2 et $\theta$ = $-2\pi/3$
    \item[false] $OM$ = 2 et $\theta$ = $\pi/2$
    \item[false] $OM$ = 6 et $\theta$ = $-3\pi/4$
    \item[true] $OM$ = 2 et $\theta$ = $11\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(-25/2,y)$ et pour rayon 25. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[true] $y$ = $-25\sqrt{3}/2$ et $\theta$ = $4\pi/3$
    \item[false] $y$ = $-75/2$ et $\theta$ = $-5\pi/6$
    \item[false] $y$ = $-25\sqrt{2}$ et $\theta$ = $-3\pi/4$
    \item[false] $y$ = $0$ et $\theta$ = $\pi$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(25, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $\pi/3$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $25\sqrt{2}$ et $OM$ = 50
    \item[false] $y$ = $50$ et $OM$ = 50
    \item[true] $y$ = $25\sqrt{3}$ et $OM$ = 50
    \item[false] $y$ = $50\sqrt{3}$ et $OM$ = 100
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 3 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $0$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[true] $x$ = $3$ et $y$ = $0$
    \item[false] $x$ = $3\sqrt{2}/2$ et $y$ = $3\sqrt{2}/2$
    \item[false] $x$ = $9\sqrt{3}/2$ et $y$ = $9/2$
    \item[false] $x$ = $3/2$ et $y$ = $3\sqrt{3}/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, -19\sqrt{3}/2)$ et pour rayon 19. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $0$ et $\theta$ = $-2\pi/3$
    \item[true] $x$ = $19/2$ et $\theta$ = $11\pi/3$
    \item[false] $x$ = $0$ et $\theta$ = $\pi/2$
    \item[false] $x$ = $0$ et $\theta$ = $-3\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 23 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $0$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $23\sqrt{2}/2$ et $y$ = $23\sqrt{2}/2$
    \item[false] $x$ = $23\sqrt{3}$ et $y$ = $23$
    \item[true] $x$ = $23$ et $y$ = $0$
    \item[false] $x$ = $23/2$ et $y$ = $23\sqrt{3}/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (180.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$};
    \pic ["$\theta=\pi$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $3\sqrt{3}$ et $OM$ = 6
    \item[false] $x$ = $3\sqrt{2}/2$ et $OM$ = 3
    \item[true] $x$ = $-3$ et $OM$ = 3
    \item[false] $x$ = $3/2$ et $OM$ = 3
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=3$, $y=0$ };
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 6 et $\theta$ = $\pi/3$
    \item[false] $OM$ = 3 et $\theta$ = $\pi/4$
    \item[true] $OM$ = 3 et $\theta$ = $0$
    \item[false] $OM$ = 3 et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $0$. Que valent le rayon $OM$ et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $33/2$ et $OM$ = 33
    \item[false] $x$ = $33\sqrt{3}/2$ et $OM$ = 33
    \item[true] $x$ = $33$ et $OM$ = 33
    \item[false] $x$ = $33\sqrt{2}/2$ et $OM$ = 33
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour rayon 17. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $17\sqrt{3}/2$ et $\theta$ = $\pi/4$
    \item[false] $x$ = $17\sqrt{3}/2$ et $\theta$ = $\pi/6$
    \item[true] $x$ = $17$ et $\theta$ = $0$
    \item[false] $x$ = $17\sqrt{3}/2$ et $\theta$ = $\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $3\pi/2$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $-33$ et $OM$ = 66
    \item[false] $y$ = $-99\sqrt{3}/2$ et $OM$ = 99
    \item[false] $y$ = $-99\sqrt{2}/2$ et $OM$ = 99
    \item[true] $y$ = $-33$ et $OM$ = 33
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (210.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-1.73) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["y=$-1$"] ++ (90:-1.0);
    \pic ["$\theta=7\pi/6$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $6$ et $OM$ = 6
    \item[false] $x$ = $-6$ et $OM$ = 6
    \item[true] $x$ = $-\sqrt{3}$ et $OM$ = 2
    \item[false] $x$ = $3\sqrt{3}$ et $OM$ = 6
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour rayon 5. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[true] $x$ = $5$ et $\theta$ = $0$
    \item[false] $x$ = $15\sqrt{3}/2$ et $\theta$ = $\pi/4$
    \item[false] $x$ = $15\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[false] $x$ = $15\sqrt{3}/2$ et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (180.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$};
    \pic ["$\theta=\pi$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $-2$ et $OM$ = 2
    \item[false] $x$ = $\sqrt{3}$ et $OM$ = 2
    \item[false] $x$ = $3$ et $OM$ = 6
    \item[false] $x$ = $\sqrt{2}$ et $OM$ = 2
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (90.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:0.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:3.0);
    \draw (1,1) node[right]{$OM=3$, $x=0$};
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $y$ = $3\sqrt{2}$ et $\theta$ = $3\pi/4$
    \item[false] $y$ = $3\sqrt{3}/2$ et $\theta$ = $2\pi/3$
    \item[false] $y$ = $3$ et $\theta$ = $5\pi/6$
    \item[true] $y$ = $3$ et $\theta$ = $\pi/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (900.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $y=0$};
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/6$
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/4$
    \item[true] $x$ = $-2$ et $\theta$ = $5\pi$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(9,y)$ et pour rayon 9. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $9\sqrt{2}/2$ et $\theta$ = $\pi/4$
    \item[true] $y$ = $0$ et $\theta$ = $0$
    \item[false] $y$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[false] $y$ = $9/2$ et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=2$",sloped] ++ (330.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:1.73) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y$"] ++ (90:-1.0);
    \pic ["$\theta=11\pi/6$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $0$ et $y$ = $-2$
    \item[true] $x$ = $\sqrt{3}$ et $y$ = $-1$
    \item[false] $x$ = $2\sqrt{2}$ et $y$ = $-2\sqrt{2}$
    \item[false] $x$ = $1$ et $y$ = $-\sqrt{3}$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (900.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=3$, $y=0$};
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $-3$ et $\theta$ = $5\pi$
    \item[false] $x$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/4$
    \item[false] $x$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[false] $x$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (60.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x=1$"] ++ (0:1.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=\sqrt{3}$"] ++ (90:1.73);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 2 et $\theta$ = $\pi/2$
    \item[false] $OM$ = 2 et $\theta$ = $3\pi/4$
    \item[true] $OM$ = 2 et $\theta$ = $\pi/3$
    \item[false] $OM$ = 4 et $\theta$ = $2\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 12 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $\pi/2$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-18\sqrt{3}$ et $y$ = $18$
    \item[true] $x$ = $0$ et $y$ = $12$
    \item[false] $x$ = $-6\sqrt{2}$ et $y$ = $6\sqrt{2}$
    \item[false] $x$ = $-12$ et $y$ = $12\sqrt{3}$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 15 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $11\pi/3$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-15\sqrt{2}/2$ et $y$ = $-15\sqrt{2}/2$
    \item[true] $x$ = $15/2$ et $y$ = $-15\sqrt{3}/2$
    \item[false] $x$ = $0$ et $y$ = $-30$
    \item[false] $x$ = $-45/2$ et $y$ = $-45\sqrt{3}/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (540.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=-2$, $y=0$ };
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 6 et $\theta$ = $\pi/3$
    \item[false] $OM$ = 4 et $\theta$ = $\pi/6$
    \item[true] $OM$ = 2 et $\theta$ = $3\pi$
    \item[false] $OM$ = 2 et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(-46, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $11\pi$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $-46$ et $OM$ = 92
    \item[false] $y$ = $-23\sqrt{3}$ et $OM$ = 46
    \item[true] $y$ = $0$ et $OM$ = 46
    \item[false] $y$ = $-23\sqrt{2}$ et $OM$ = 46
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (1980.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=-3$, $y=0$ };
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 3 et $\theta$ = $\pi/6$
    \item[true] $OM$ = 3 et $\theta$ = $11\pi$
    \item[false] $OM$ = 3 et $\theta$ = $\pi/4$
    \item[false] $OM$ = 6 et $\theta$ = $\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (90.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:0.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:3.0);
    \draw (1,1) node[right]{$x=0$, $y=3$ };
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 3 et $\theta$ = $3\pi/4$
    \item[true] $OM$ = 3 et $\theta$ = $\pi/2$
    \item[false] $OM$ = 9 et $\theta$ = $2\pi/3$
    \item[false] $OM$ = 6 et $\theta$ = $5\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=3$, $x=3$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $y$ = $0$ et $\theta$ = $0$
    \item[false] $y$ = $3$ et $\theta$ = $\pi/6$
    \item[false] $y$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[false] $y$ = $9\sqrt{2}/2$ et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (1260.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=3$, $x=-3$};
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $y$ = $0$ et $\theta$ = $7\pi$
    \item[false] $y$ = $-3/2$ et $\theta$ = $\pi/6$
    \item[false] $y$ = $-3\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[false] $y$ = $-3\sqrt{2}/2$ et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour rayon 46. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $69\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[false] $x$ = $69\sqrt{3}$ et $\theta$ = $\pi/4$
    \item[false] $x$ = $69\sqrt{3}$ et $\theta$ = $\pi/6$
    \item[true] $x$ = $-46$ et $\theta$ = $\pi$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=3$",sloped] ++ (30.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:2.59) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y$"] ++ (90:1.5);
    \pic ["$\theta=\pi/6$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $9\sqrt{2}/2$ et $y$ = $9\sqrt{2}/2$
    \item[false] $x$ = $0$ et $y$ = $3$
    \item[false] $x$ = $3/2$ et $y$ = $3\sqrt{3}/2$
    \item[true] $x$ = $3\sqrt{3}/2$ et $y$ = $3/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $y=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[true] $x$ = $2$ et $\theta$ = $0$
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/6$
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(13, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $0$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $39/2$ et $OM$ = 39
    \item[false] $y$ = $13\sqrt{3}/2$ et $OM$ = 13
    \item[false] $y$ = $13\sqrt{2}/2$ et $OM$ = 13
    \item[true] $y$ = $0$ et $OM$ = 13
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour rayon 18. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $9\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[true] $x$ = $-18$ et $\theta$ = $\pi$
    \item[false] $x$ = $9\sqrt{3}$ et $\theta$ = $\pi/6$
    \item[false] $x$ = $9\sqrt{3}$ et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (480.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-1.5) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["y=$3\sqrt{3}/2$"] ++ (90:2.59);
    \pic ["$\theta=8\pi/3$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $-3/2$ et $OM$ = 3
    \item[false] $x$ = $-6$ et $OM$ = 6
    \item[false] $x$ = $-3\sqrt{2}$ et $OM$ = 6
    \item[false] $x$ = $-3\sqrt{3}$ et $OM$ = 6
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour rayon 37. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $37\sqrt{3}$ et $\theta$ = $\pi/4$
    \item[false] $x$ = $37\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[true] $x$ = $-37$ et $\theta$ = $9\pi$
    \item[false] $x$ = $37\sqrt{3}$ et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=2$",sloped] ++ (135.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-1.41) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=\sqrt{2}$"] ++ (90:1.41);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $-3\sqrt{3}$ et $\theta$ = $5\pi/6$
    \item[true] $x$ = $-\sqrt{2}$ et $\theta$ = $3\pi/4$
    \item[false] $x$ = $-3\sqrt{3}$ et $\theta$ = $\pi/6$
    \item[false] $x$ = $-3\sqrt{3}$ et $\theta$ = $\pi$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(21\sqrt{2}/2,-21\sqrt{2}/2)$. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $OM$ = 42 et $\theta$ = $\pi/3$
    \item[false] $OM$ = 63 et $\theta$ = $-2\pi/3$
    \item[false] $OM$ = 21 et $\theta$ = $\pi/2$
    \item[true] $OM$ = 21 et $\theta$ = $7\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, -14)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $7\pi/2$. Que valent le rayon $OM$ et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-7\sqrt{2}$ et $OM$ = 14
    \item[true] $x$ = $0$ et $OM$ = 14
    \item[false] $x$ = $-7\sqrt{3}$ et $OM$ = 14
    \item[false] $x$ = $-7$ et $OM$ = 14
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(-35,y)$ et pour rayon 35. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $-105\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[false] $y$ = $-35\sqrt{2}/2$ et $\theta$ = $\pi/4$
    \item[true] $y$ = $0$ et $\theta$ = $3\pi$
    \item[false] $y$ = $-35/2$ et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 44 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $4\pi/3$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-66\sqrt{3}$ et $y$ = $-66$
    \item[true] $x$ = $-22$ et $y$ = $-22\sqrt{3}$
    \item[false] $x$ = $-22\sqrt{2}$ et $y$ = $-22\sqrt{2}$
    \item[false] $x$ = $-132$ et $y$ = $0$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (90.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:0.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:3.0);
    \draw (1,1) node[right]{$OM=3$, $y=3$};
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $-9/2$ et $\theta$ = $2\pi/3$
    \item[false] $x$ = $-9/2$ et $\theta$ = $3\pi/4$
    \item[false] $x$ = $-9/2$ et $\theta$ = $5\pi/6$
    \item[true] $x$ = $0$ et $\theta$ = $\pi/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(-15,0)$. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $OM$ = 15 et $\theta$ = $\pi/4$
    \item[false] $OM$ = 15 et $\theta$ = $\pi/6$
    \item[true] $OM$ = 15 et $\theta$ = $\pi$
    \item[false] $OM$ = 45 et $\theta$ = $\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 4 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $11\pi/2$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-2\sqrt{3}$ et $y$ = $-2$
    \item[true] $x$ = $0$ et $y$ = $-4$
    \item[false] $x$ = $-6\sqrt{2}$ et $y$ = $-6\sqrt{2}$
    \item[false] $x$ = $-2$ et $y$ = $-2\sqrt{3}$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $2\sqrt{2}$ et $y$ = $2\sqrt{2}$
    \item[true] $x$ = $2$ et $y$ = $0$
    \item[false] $x$ = $2\sqrt{3}$ et $y$ = $2$
    \item[false] $x$ = $3$ et $y$ = $3\sqrt{3}$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0,y)$ et pour rayon 40. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $-40\sqrt{2}$ et $\theta$ = $-3\pi/4$
    \item[true] $y$ = $-40$ et $\theta$ = $3\pi/2$
    \item[false] $y$ = $-20$ et $\theta$ = $-5\pi/6$
    \item[false] $y$ = $-60\sqrt{3}$ et $\theta$ = $-2\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 29 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $5\pi/3$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $0$ et $y$ = $-29$
    \item[true] $x$ = $29/2$ et $y$ = $-29\sqrt{3}/2$
    \item[false] $x$ = $-87\sqrt{2}/2$ et $y$ = $-87\sqrt{2}/2$
    \item[false] $x$ = $-87/2$ et $y$ = $-87\sqrt{3}/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $x=2$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $y$ = $3$ et $\theta$ = $\pi/6$
    \item[false] $y$ = $\sqrt{2}$ et $\theta$ = $\pi/4$
    \item[false] $y$ = $2\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[true] $y$ = $0$ et $\theta$ = $0$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=2$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $y$ = $2\sqrt{3}$ et $OM$ = 4
    \item[false] $y$ = $2\sqrt{2}$ et $OM$ = 4
    \item[true] $y$ = $0$ et $OM$ = 2
    \item[false] $y$ = $2$ et $OM$ = 4
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $y=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[true] $x$ = $2$ et $\theta$ = $0$
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/6$
    \item[false] $x$ = $\sqrt{3}$ et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (120.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-1.5) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["y=$3\sqrt{3}/2$"] ++ (90:2.59);
    \pic ["$\theta=2\pi/3$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $-3\sqrt{3}$ et $OM$ = 6
    \item[true] $x$ = $-3/2$ et $OM$ = 3
    \item[false] $x$ = $-9\sqrt{2}/2$ et $OM$ = 9
    \item[false] $x$ = $-3$ et $OM$ = 3
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=3$, $y=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $3$ et $\theta$ = $0$
    \item[false] $x$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/4$
    \item[false] $x$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[false] $x$ = $9\sqrt{3}/2$ et $\theta$ = $\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (270.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:0.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:-3.0);
    \draw (1,1) node[right]{$x=0$, $y=-3$ };
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 3 et $\theta$ = $-2\pi/3$
    \item[false] $OM$ = 3 et $\theta$ = $-3\pi/4$
    \item[true] $OM$ = 3 et $\theta$ = $3\pi/2$
    \item[false] $OM$ = 3 et $\theta$ = $-5\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $3$ et $OM$ = 3
    \item[false] $x$ = $3/2$ et $OM$ = 3
    \item[false] $x$ = $9\sqrt{2}/2$ et $OM$ = 9
    \item[false] $x$ = $3\sqrt{3}/2$ et $OM$ = 3
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 50 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $9\pi/2$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-25\sqrt{3}$ et $y$ = $25$
    \item[false] $x$ = $-75$ et $y$ = $75\sqrt{3}$
    \item[false] $x$ = $-25\sqrt{2}$ et $y$ = $25\sqrt{2}$
    \item[true] $x$ = $0$ et $y$ = $50$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (45.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x=3\sqrt{2}/2$"] ++ (0:2.12) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=3\sqrt{2}/2$"] ++ (90:2.12);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 3 et $\theta$ = $\pi/3$
    \item[true] $OM$ = 3 et $\theta$ = $\pi/4$
    \item[false] $OM$ = 6 et $\theta$ = $2\pi/3$
    \item[false] $OM$ = 9 et $\theta$ = $\pi/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 43 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $5\pi$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[true] $x$ = $-43$ et $y$ = $0$
    \item[false] $x$ = $43/2$ et $y$ = $-43\sqrt{3}/2$
    \item[false] $x$ = $129\sqrt{2}/2$ et $y$ = $-129\sqrt{2}/2$
    \item[false] $x$ = $43\sqrt{3}$ et $y$ = $-43$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $3\pi$. Que valent le rayon $OM$ et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $48\sqrt{3}$ et $OM$ = 96
    \item[true] $x$ = $-48$ et $OM$ = 48
    \item[false] $x$ = $48\sqrt{2}$ et $OM$ = 96
    \item[false] $x$ = $72$ et $OM$ = 144
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0,y)$ et pour rayon 21. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $21\sqrt{2}/2$ et $\theta$ = $3\pi/4$
    \item[false] $y$ = $21\sqrt{3}$ et $\theta$ = $2\pi/3$
    \item[false] $y$ = $21$ et $\theta$ = $5\pi/6$
    \item[true] $y$ = $21$ et $\theta$ = $9\pi/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(-19/2, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $2\pi/3$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $0$ et $OM$ = 19
    \item[true] $y$ = $19\sqrt{3}/2$ et $OM$ = 19
    \item[false] $y$ = $19/2$ et $OM$ = 19
    \item[false] $y$ = $57\sqrt{2}/2$ et $OM$ = 57
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0,-8)$. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $OM$ = 8 et $\theta$ = $-2\pi/3$
    \item[true] $OM$ = 8 et $\theta$ = $3\pi/2$
    \item[false] $OM$ = 8 et $\theta$ = $-5\pi/6$
    \item[false] $OM$ = 8 et $\theta$ = $-3\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0,49)$. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $OM$ = 98 et $\theta$ = $2\pi/3$
    \item[false] $OM$ = 49 et $\theta$ = $3\pi/4$
    \item[false] $OM$ = 49 et $\theta$ = $5\pi/6$
    \item[true] $OM$ = 49 et $\theta$ = $5\pi/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $3\sqrt{2}/2$ et $OM$ = 3
    \item[false] $x$ = $3\sqrt{3}/2$ et $OM$ = 3
    \item[true] $x$ = $3$ et $OM$ = 3
    \item[false] $x$ = $9/2$ et $OM$ = 9
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $2\sqrt{2}$ et $y$ = $2\sqrt{2}$
    \item[false] $x$ = $3$ et $y$ = $3\sqrt{3}$
    \item[false] $x$ = $2\sqrt{3}$ et $y$ = $2$
    \item[true] $x$ = $2$ et $y$ = $0$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (1260.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=-2$};
    \pic ["$\theta=7\pi$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $y$ = $-\sqrt{2}$ et $OM$ = 2
    \item[false] $y$ = $-\sqrt{3}$ et $OM$ = 2
    \item[true] $y$ = $0$ et $OM$ = 2
    \item[false] $y$ = $-1$ et $OM$ = 2
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $\sqrt{3}$ et $OM$ = 2
    \item[true] $x$ = $2$ et $OM$ = 2
    \item[false] $x$ = $2$ et $OM$ = 4
    \item[false] $x$ = $\sqrt{2}$ et $OM$ = 2
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (810.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:0.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:2.0);
    \draw (1,1) node[right]{$OM=2$};
    \pic ["$\theta=9\pi/2$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $0$ et $y$ = $2$
    \item[false] $x$ = $-\sqrt{2}$ et $y$ = $\sqrt{2}$
    \item[false] $x$ = $-3\sqrt{3}$ et $y$ = $3$
    \item[false] $x$ = $-3$ et $y$ = $3\sqrt{3}$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (540.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=-3$, $y=0$ };
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 6 et $\theta$ = $\pi/6$
    \item[false] $OM$ = 3 et $\theta$ = $\pi/4$
    \item[true] $OM$ = 3 et $\theta$ = $3\pi$
    \item[false] $OM$ = 9 et $\theta$ = $\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $3\pi$. Que valent le rayon $OM$ et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $29\sqrt{2}$ et $OM$ = 58
    \item[false] $x$ = $29/2$ et $OM$ = 29
    \item[false] $x$ = $87\sqrt{3}/2$ et $OM$ = 87
    \item[true] $x$ = $-29$ et $OM$ = 29
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$x=3$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $y$ = $3$ et $OM$ = 6
    \item[false] $y$ = $3\sqrt{3}/2$ et $OM$ = 3
    \item[false] $y$ = $3\sqrt{2}/2$ et $OM$ = 3
    \item[true] $y$ = $0$ et $OM$ = 3
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=3$",sloped] ++ (225.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-2.12) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y$"] ++ (90:-2.12);
    \pic ["$\theta=5\pi/4$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $6$ et $y$ = $0$
    \item[true] $x$ = $-3\sqrt{2}/2$ et $y$ = $-3\sqrt{2}/2$
    \item[false] $x$ = $-6$ et $y$ = $0$
    \item[false] $x$ = $-3\sqrt{3}/2$ et $y$ = $-3/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (240.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x=-3/2$"] ++ (0:-1.5) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=-3\sqrt{3}/2$"] ++ (90:-2.59);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 3 et $\theta$ = $\pi$
    \item[false] $OM$ = 9 et $\theta$ = $-3\pi/4$
    \item[true] $OM$ = 3 et $\theta$ = $4\pi/3$
    \item[false] $OM$ = 6 et $\theta$ = $-5\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (900.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:-2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$};
    \pic ["$\theta=5\pi$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $1$ et $OM$ = 2
    \item[false] $x$ = $\sqrt{3}$ et $OM$ = 2
    \item[false] $x$ = $2\sqrt{2}$ et $OM$ = 4
    \item[true] $x$ = $-2$ et $OM$ = 2
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (30.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x=\sqrt{3}$"] ++ (0:1.73) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y$"] ++ (90:1.0);
    \pic ["$\theta=\pi/6$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $y$ = $6$ et $OM$ = 6
    \item[false] $y$ = $3\sqrt{3}$ et $OM$ = 6
    \item[true] $y$ = $1$ et $OM$ = 2
    \item[false] $y$ = $2\sqrt{2}$ et $OM$ = 4
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 5\sqrt{3})$ et pour rayon 10. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-10\sqrt{2}$ et $\theta$ = $3\pi/4$
    \item[false] $x$ = $-10\sqrt{2}$ et $\theta$ = $\pi$
    \item[true] $x$ = $-5$ et $\theta$ = $2\pi/3$
    \item[false] $x$ = $-10\sqrt{2}$ et $\theta$ = $5\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 0)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $\pi$. Que valent le rayon $OM$ et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $6$ et $OM$ = 12
    \item[false] $x$ = $4\sqrt{2}$ et $OM$ = 8
    \item[false] $x$ = $2\sqrt{3}$ et $OM$ = 4
    \item[true] $x$ = $-4$ et $OM$ = 4
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=3$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $3$ et $y$ = $0$
    \item[false] $x$ = $3$ et $y$ = $3\sqrt{3}$
    \item[false] $x$ = $3\sqrt{3}$ et $y$ = $3$
    \item[false] $x$ = $3\sqrt{2}/2$ et $y$ = $3\sqrt{2}/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0,y)$ et pour rayon 14. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[true] $y$ = $-14$ et $\theta$ = $3\pi/2$
    \item[false] $y$ = $-7\sqrt{3}$ et $\theta$ = $-2\pi/3$
    \item[false] $y$ = $-21$ et $\theta$ = $-5\pi/6$
    \item[false] $y$ = $-7\sqrt{2}$ et $\theta$ = $-3\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $2$ et $OM$ = 2
    \item[false] $x$ = $\sqrt{3}$ et $OM$ = 2
    \item[false] $x$ = $2$ et $OM$ = 4
    \item[false] $x$ = $\sqrt{2}$ et $OM$ = 2
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(x, 13)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $\pi/2$. Que valent le rayon $OM$ et la coordonnée $x$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-13/2$ et $OM$ = 13
    \item[false] $x$ = $-39\sqrt{3}/2$ et $OM$ = 39
    \item[true] $x$ = $0$ et $OM$ = 13
    \item[false] $x$ = $-39\sqrt{2}/2$ et $OM$ = 39
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 2 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $3\pi$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $2$ et $y$ = $-2\sqrt{3}$
    \item[true] $x$ = $-2$ et $y$ = $0$
    \item[false] $x$ = $2\sqrt{2}$ et $y$ = $-2\sqrt{2}$
    \item[false] $x$ = $\sqrt{3}$ et $y$ = $-1$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour rayon 28 et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $5\pi/2$. Que valent les coordonnées $x$ et $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $x$ = $-28\sqrt{2}$ et $y$ = $28\sqrt{2}$
    \item[true] $x$ = $0$ et $y$ = $28$
    \item[false] $x$ = $-42$ et $y$ = $42\sqrt{3}$
    \item[false] $x$ = $-28\sqrt{3}$ et $y$ = $28$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $3\sqrt{2}/2$ et $OM$ = 3
    \item[false] $x$ = $9\sqrt{3}/2$ et $OM$ = 9
    \item[false] $x$ = $3/2$ et $OM$ = 3
    \item[true] $x$ = $3$ et $OM$ = 3
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $x$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=2$",sloped] ++ (240.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-1.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=-\sqrt{3}$"] ++ (90:-1.73);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $-\sqrt{2}$ et $\theta$ = $-3\pi/4$
    \item[false] $x$ = $-\sqrt{2}$ et $\theta$ = $\pi$
    \item[true] $x$ = $-1$ et $\theta$ = $4\pi/3$
    \item[false] $x$ = $-\sqrt{2}$ et $\theta$ = $-5\pi/6$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $\pi/2$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $41\sqrt{3}/2$ et $OM$ = 41
    \item[false] $y$ = $41/2$ et $OM$ = 41
    \item[true] $y$ = $41$ et $OM$ = 41
    \item[false] $y$ = $41\sqrt{2}$ et $OM$ = 82
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent le rayon $OM$ et la coordonnée $x$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:3.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$y=0$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $3$ et $OM$ = 3
    \item[false] $x$ = $3\sqrt{2}/2$ et $OM$ = 3
    \item[false] $x$ = $3$ et $OM$ = 6
    \item[false] $x$ = $3\sqrt{3}/2$ et $OM$ = 3
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Parmis les valeurs ci-dessous, lesquelles peuvent être égales à l'angle $\theta$ (entre le vecteur et l'axe des $x$) et le rayon $OM$ du vecteur $\vec{OM}$? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (210.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x=-3\sqrt{3}/2$"] ++ (0:-2.59) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y=-3/2$"] ++ (90:-1.5);
    \pic ["$\theta$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $OM$ = 6 et $\theta$ = $\pi/6$
    \item[true] $OM$ = 3 et $\theta$ = $7\pi/6$
    \item[false] $OM$ = 3 et $\theta$ = $0$
    \item[false] $OM$ = 3 et $\theta$ = $\pi$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(9,y)$ et pour rayon 9. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $9\sqrt{2}/2$ et $\theta$ = $\pi/4$
    \item[false] $y$ = $9\sqrt{3}$ et $\theta$ = $\pi/3$
    \item[false] $y$ = $9$ et $\theta$ = $\pi/6$
    \item[true] $y$ = $0$ et $\theta$ = $0$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=3$",sloped] ++ (240.0:3) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:-1.5) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y$"] ++ (90:-2.59);
    \pic ["$\theta=4\pi/3$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $-3/2$ et $y$ = $-3\sqrt{3}/2$
    \item[false] $x$ = $-3$ et $y$ = $0$
    \item[false] $x$ = $-3\sqrt{2}/2$ et $y$ = $-3\sqrt{2}/2$
    \item[false] $x$ = $-3\sqrt{3}/2$ et $y$ = $-3/2$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(-29,y)$ et pour rayon 29. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $-29$ et $\theta$ = $\pi/6$
    \item[false] $y$ = $-87\sqrt{3}/2$ et $\theta$ = $\pi/3$
    \item[true] $y$ = $0$ et $\theta$ = $3\pi$
    \item[false] $y$ = $-87\sqrt{2}/2$ et $\theta$ = $\pi/4$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to ["$OM=2$",sloped] ++ (60.0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to ["$x$"] ++ (0:1.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to ["$y$"] ++ (90:1.73);
    \pic ["$\theta=\pi/3$", draw=red,->, angle radius = 12mm, angle eccentricity=1.3] {angle = x--O--M};
\end{tikzpicture}
\end{question}

\begin{reponses}
    \item[false] $x$ = $-\sqrt{2}$ et $y$ = $\sqrt{2}$
    \item[true] $x$ = $1$ et $y$ = $\sqrt{3}$
    \item[false] $x$ = $0$ et $y$ = $2$
    \item[false] $x$ = $-1$ et $y$ = $\sqrt{3}$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(0,y)$ et pour rayon 21. Que valent l'angle $\theta$ (entre le vecteur et l'axe des $x$) et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $21\sqrt{2}/2$ et $\theta$ = $3\pi/4$
    \item[true] $y$ = $21$ et $\theta$ = $5\pi/2$
    \item[false] $y$ = $21/2$ et $\theta$ = $5\pi/6$
    \item[false] $y$ = $63\sqrt{3}/2$ et $\theta$ = $2\pi/3$
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Un vecteur a pour coordonnées $(15, y)$ et pour angle $\theta$ (entre le vecteur et l'axe des $x$) $0$. Que valent le rayon $OM$ et la coordonnée $y$ de ce vecteur?
\end{question}

\begin{reponses}
    \item[false] $y$ = $15\sqrt{3}/2$ et $OM$ = 15
    \item[false] $y$ = $45/2$ et $OM$ = 45
    \item[false] $y$ = $45\sqrt{2}/2$ et $OM$ = 45
    \item[true] $y$ = $0$ et $OM$ = 15
\end{reponses}

\begin{question}{/}{Vecteurs}{2}{1217,31,1213}
    Que valent les coordonnées $x$ et $y$ du vecteur $\vec{OM}$ ci-dessous? \\ 
\begin{tikzpicture}[scale=3, axis/.style={->,blue,thick}, vector/.style={-stealth,red,very thick}, vector guide/.style={dashed,red,thick}]
    \coordinate[label=below  left:O] (O) at (0cm,0cm);
    \coordinate[label=below  left:] (x) at (1cm,0cm);
    \draw[axis] (0,0) -- (1,0) node[anchor=north east]{$x$};
    \draw[axis] (0,0) -- (0,1) node[anchor=north west]{$y$};
    \draw[-Stealth] (O) to [] ++ (0:2) coordinate[label=above right:M] (M);
    \draw[densely dotted] (O) to [] ++ (0:2.0) coordinate (Mx);
    \draw[densely dotted] (Mx) to [] ++ (90:0.0);
    \draw (1,1) node[right]{$OM=2$, $\theta=0$};
    \end{tikzpicture}
\end{question}

\begin{reponses}
    \item[true] $x$ = $2$ et $y$ = $0$
    \item[false] $x$ = $\sqrt{2}$ et $y$ = $\sqrt{2}$
    \item[false] $x$ = $2$ et $y$ = $2\sqrt{3}$
    \item[false] $x$ = $\sqrt{3}$ et $y$ = $1$
\end{reponses}

\end{document}
